<a href="#/artists/<%= artist.id %>"><%= artist.escape('name') %></a>
<% if (ids && (ids.indexOf(artist.id) === -1)) { %>
<button class="track track-artist">Track artist</button>
<% } else { %>
<button class="untrack untrack-artist">Stop tracking</button>
<% } %>
