<h1><%= locale.escape('city') %>, <%= locale.escape('state') %></h1>
<% if (ids && (ids.indexOf(locale.id) === -1)) { %>
<button class="track track-locale">Track Location</button>
<% } else { %>
<button class="untrack untrack-locale">Stop tracking</button>
<% } %>
<h2>Upcoming Concerts: (<%= locale.concerts().length %>)</h2>
